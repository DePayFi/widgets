export default ()=>{
  return(`

    .Icon.white {
      fill: white;
      stroke: white;
    }

    .ChevronLeft, .ChevronRight {
      position: relative;
      top: 1px;
    }

    .Checkmark {
      height: 1.4rem;
      position: relative;
      top: -1px;
      vertical-align: middle;
      width: 1.4rem;
    }
    
  `)
}
