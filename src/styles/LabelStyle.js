export default (style)=>{
  return(`

    .Label {
      background: rgb(248,248,248);
      border-radius: 999px;
      color: ${style.colors.primary};
      font-size: 0.8rem;
      padding: 0.1rem 0.5rem;
      margin: 0.1rem;
    }

  `)
}
