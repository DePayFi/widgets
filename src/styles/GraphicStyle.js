export default ()=>{
  return(`

    .GraphicWrapper {
      display: block;
    }

    .Graphic {
      width: 60%;
      position: relative;
      top: -10px;
      margin-bottom: -10px;
    }
  `)
}
