const CircularButtonCSS = `
  .CircularButton {
    border-radius: 99rem;
    cursor: pointer;
    height: 2.4rem;
    opacity: 0.35;
    padding: 0.45rem 0.4rem 0.4rem 0.45rem;
    width: 2.4rem;
  }

  .CircularButton:hover {
    background: rgba(0,0,0,0.1);
    opacity: 1;
  }

  .CircularButton:active {
    background: rgba(0,0,0,0.25);
    opacity: 1;
  }
`;

export default CircularButtonCSS;
